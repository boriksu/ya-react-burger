type IngredientType = "bun" | "sauce" | "main";

export const INGREDIENT_TYPES: {
  BUN: IngredientType;
  SAUCE: IngredientType;
  MAIN: IngredientType;
} = {
  BUN: "bun",
  SAUCE: "sauce",
  MAIN: "main",
};
